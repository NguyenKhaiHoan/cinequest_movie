import 'package:flutter/material.dart';

class AccountSetupForthProcess extends StatelessWidget {
  const AccountSetupForthProcess({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: const Center(
        child: Text('Account Setup Forth'),
      ),
    );
  }
}
