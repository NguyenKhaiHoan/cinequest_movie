import 'package:flutter/material.dart';

class AccountSetupFirstProcess extends StatelessWidget {
  const AccountSetupFirstProcess({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: const Center(
        child: Text('Account Setup First'),
      ),
    );
  }
}
