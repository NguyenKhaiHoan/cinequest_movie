import 'package:flutter/material.dart';

class AccountSetupThirdProcess extends StatelessWidget {
  const AccountSetupThirdProcess({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: const Center(
        child: Text('Account Setup Third'),
      ),
    );
  }
}
