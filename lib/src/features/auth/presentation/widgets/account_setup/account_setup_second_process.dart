import 'package:flutter/material.dart';

class AccountSetupSecondProcess extends StatelessWidget {
  const AccountSetupSecondProcess({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: const Center(
        child: Text('Account Setup Second'),
      ),
    );
  }
}
